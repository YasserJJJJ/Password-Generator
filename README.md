# Password-Generator
Clean, secure Password Generator built with Vite + React and vanilla CSS. Uses Web Crypto for strong RNG, entropy-based strength meter, copy to clipboard, show/hide, length slider (4–128), and customizable sets (lower/upper/numbers/symbols) with “exclude ambiguous”. Accessible, responsive UI + keyboard shortcuts.
